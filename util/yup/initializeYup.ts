/* import { setLocale } from 'yup'; */

// TODO: Find all translatable strings

/* setLocale({
  mixed: {
    default: 'Não é válido',
  },
  number: {
    min: 'Deve ser maior que ${min}',
  },
});
 */
